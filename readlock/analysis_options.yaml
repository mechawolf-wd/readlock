# This file configures the analyzer, which statically analyzes Dart code to
# check for errors, warnings, and lints.
#
# The issues identified by the analyzer are surfaced in the UI of Dart-enabled
# IDEs (https://dart.dev/tools#ides-and-editors). The analyzer can also be
# invoked from the command line by running `flutter analyze`.

# The following line activates a set of recommended lints for Flutter apps,
# packages, and plugins designed to encourage good coding practices.
include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "**/*.mocks.dart"

linter:
  rules:
    # Style rules
    prefer_single_quotes: true
    curly_braces_in_flow_control_structures: true
    always_put_control_body_on_new_line: true
    unnecessary_new: true
    unnecessary_this: true
    
    # Performance rules
    prefer_const_constructors: true
    prefer_const_literals_to_create_immutables: true
    prefer_collection_literals: true
    prefer_is_empty: true
    prefer_is_not_empty: true
    
    # Flutter-specific rules
    avoid_unnecessary_containers: true
    sized_box_for_whitespace: true
    use_full_hex_values_for_flutter_colors: true
    
    # Code quality rules
    prefer_final_fields: true
    prefer_final_locals: true
    avoid_redundant_argument_values: true
    
    # Safety rules
    prefer_null_aware_operators: true
    avoid_null_checks_in_equality_operators: true
    avoid_dynamic_calls: true
    avoid_type_to_string: true
    avoid_catches_without_on_clauses: true
    avoid_catching_errors: true
    
    # Naming conventions (aligned with CLAUDE.md)
    camel_case_types: true
    constant_identifier_names: false
    
    # Code structure (aligned with CLAUDE.md)
    always_declare_return_types: true
    type_annotate_public_apis: true
    prefer_function_declarations_over_variables: true
    
    # Best practices (aligned with CLAUDE.md)
    avoid_empty_else: true
    avoid_returning_null_for_void: true
    use_string_buffers: true
    unnecessary_lambdas: true
    
    # Disabled rules for project-specific style
    non_constant_identifier_names: false  # Allow capitalized widget method names

# Additional information about this file can be found at
# https://dart.dev/guides/language/analysis-options
